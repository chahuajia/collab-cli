# Decisions

> 决策日志——记录"何时做了决定"的历史。**与 working-memory 不同**：working-memory 描述当前状态，本文件记录历史决策。

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-13 | 暂不引入"思元" | 先有固定核心再谈自举 | 生效中 |
| 2026-09-13 | 用 MCP 而非打包脚本做工具桥接 | 长期价值 | 待触发 |
| 2026-09-13 | working-memory 按任务分（非按人） | 任务可交接，人不可交接 | 生效中 |
| 2026-09-13 | 知识笔记主题直接 H2（去"知识笔记："前缀） | 独立上下文，分类由用户负责 | 生效中 |
| 2026-09-13 | 分类是侧重不是隔离（A14 修正） | 知识实体既读又写 | 生效中 |
| 2026-09-13 | 暂不引入二维知识模型（项目/组织维度） | 当前单项目，过度设计 | 生效中 |
| 2026-09-13 | 采用 A16 上下文预算法 | 上下文是预算不是容器 | 生效中 |

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-14 | 任务 #3：`generateNextId` 拆为"IO + 纯逻辑" | 分层正确 + 可测试 | 生效中 |
| 2026-09-14 | 任务 #4：`collectAllEntryIds` 合并到 `extractAllIdsByKind` | 任务 #3 产出让 #4 变了 | 生效中 |
| 2026-09-14 | `extractAllIdsByKind` 复用 `extractIdsForKind`（不优化为一次遍历） | 数据量小 + 单一真相源 | 生效中 |
| 2026-09-14 | 测试数据从 path 推断 type（`inferTypeFromPath`） | 减少手工 + 防遗漏 | 生效中 |

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-16 | **`collaboration` 保持文档纯洁**：工具脚本与工作产物归 `collab-cli/working-memory` | 避免两个仓库各有一份工具真相源；工作空间收敛到一处 | 生效中 |
| 2026-09-16 | 先修校验（206 → 0）再谈新功能 | 校验不绿时，任何"新增"都在污染基线 | 已完成 |

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-16 | `collab apply` 落盘门禁用 `contentRules`（不含索引规则） | 新建条目还没进 `_index.md`；全量 validate 会让"每次新建落盘"都失败 | 生效中 |
| 2026-09-16 | `standardRules = contentRules + 索引规则`（组合而非并列两份） | 避免"内容规则"出现两份、各自漂移 | 生效中 |
| 2026-09-16 | 哈希以函数注入（`ContentHasher`），领域不 import `node:crypto` | How 可封装：领域声明"要一个哈希"，算法由基础设施给 | 生效中 |
| 2026-09-16 | `apply` 预检拆两段：`validateBundle`（纯）+ `resolvePlan`（纯，吃事实快照） | 冲突规则可用表驱动测试；IO 只在观察与执行两处 | 生效中 |
| 2026-09-16 | `apply` 的 path 白名单含 `inbox/` 与 `domains/`；不强制 `.md` 后缀 | 沿用 spec §1 的允许目录；`.md` 要求未被提出，不擅自加 | 生效中 |
| 2026-09-16 | E2E 超时预算 5s → 20s（`vitest.config.ts`） | 每条 E2E 都要 `git init` + 起 node 子进程；并行下 4-7s，超时是预算问题不是正确性问题 | 生效中 |

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-16 | **引用匹配规则只留一份**：`refersToIdentity`（domain），`renderIndex` 复用 | 实测：两份规则导致 `collab index` 删掉真库 54 行人工列 | 生效中 |
| 2026-09-16 | `extractAllIdsByKind` → `extractAllIndexEntries`（带 `fileName`）；`extractIdsForKind` 改为派生 | 判断"已有行是否悬空"需要文件名，只给 id 会重演同一事故 | 生效中 |
| 2026-09-16 | working-memory 里的**校验结论必须附可复现命令** | "108 entries / 0 issues" 是过期断言，实测 2 errors —— 断言会腐烂，命令不会 | 生效中 |
| 2026-09-16 | **AI 不擅自替用户拍 spec 的未决问题**；未决项要镜像进 `decisions.md` 标"待拍板" | 上一轮 `apply` 我按建议默认值做了；这次先交规格等拍板，避免"猜方向"的返工 | 待用户确认 |

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-16 | **"承诺 vs 现实"成为一等核查项**：文档承诺的命令/文件/字段，必须能在现实里找到 | 实测：S10 把不存在的 `init`/`propose`/`pr`/`sync` 写成使用说明；`CODEOOWNERS` 引用不存在的 `@core-team`。这比死链更毒 —— 它在教 AI 引用不存在的东西 | 生效中 |
| 2026-09-16 | 删除 `collaboration/.github/workflows/evolution.yml`（`echo TODO` 假绿灯） | 五项校验一项未做，且只在 PR 触发、5 次提交全是直推 → 从未运行。**虚假的绿灯比没有 CI 更糟**。校验规则只有一份（在 `collab validate`），CI 等 collab-cli 可分发后再接 | 生效中 |
| 2026-09-16 | `.idea/` 退出跟踪（已在 `.gitignore` 中） | 被跟踪的 IDE 配置会持续产生噪音 diff | 生效中 |
| 2026-09-16 | 五份外部评估（codex / ds / claude / deepseek-harness / 批判）视为**第一次外部选择压力** | 它们独立收敛到同一诊断："变异 107 次，选择 0 次"。这是系统第一次被外部检验 | 待用户确认 |

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-16 | **合并 A10 / W8 / S27 / review-marginal-value 四条为 A10**（320 行 → 约 130 行） | 四条讲同一件事（逐字级重复：优先级表 ×4、五阶段 ×2、五对策 ×3、意图论 ×4）；`pruning-policy` 规定"重叠 ≥50% 合并"却从未执行 | 已完成 |
| 2026-09-16 | 合并时**删除**原文里"边际价值增加 3-5 倍""等价于读 1000 行代码" | 编造的数字违反 A8/A9；`review-marginal-value` 自己就引用了跨领域出处当权威 | 已完成 |
| 2026-09-16 | 合并后**保留 `A10` 这个 id / 文件名**，旧编号进 `aliases` | A10 是被引用最多的条目（20 处）；改名会让 20 处引用一起动 | 已完成 |
| 2026-09-16 | 用户裁决顺序：**先合并那 4 条，再拆协议/手册** | 合并是"第一次真正的删除事件"，做成一次后后面的拆分都变成"照做" | 已确认 |

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-16 | **链接按文件名解析，不按 `frontmatter.id` 解析** | 用户实测发现：渲染层（编辑器/静态站点）只认文件名。而 `meta/naming-conventions.md` 教的是 `[[ID]]`，且验收从未检查 —— 于是"工具说合法、人点不开"的链接可以静默存在 | 生效中 |
| 2026-09-16 | 新增验收规则 `ID_FILE_NAME_MISMATCH`：`id` 必须是**文件名的前缀**（`<id>.md` 或 `<id>-<slug>.md`） | 让"链接能从文件名推出来"成为**可验收的不变量**，而不是靠自觉。用 `-` 为界避免 `S2` 命中 `S20-...` 的歧义 | 生效中 |
| 2026-09-16 | `renderIndex` 新行改用**文件名**（不再用 id） | 否则工具自己生产的索引行违反链接规则 —— 工具与规范互相打架 | 生效中 |
| 2026-09-16 | `collab new` 的 `author` 默认取 `git config user.name`（兜底 `user.email`） | `author` 的语义是"人"（真实库写的是 `heiniao`，不是邮箱）；6 个模板占位符同步改 `<git user.name>` | 生效中 |

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-16 | **`agreements/` 的定义收窄为"协作规则"**：只放"人和 AI 怎么一起工作里，不可谈判且能自查"的规则 | 它同时装着六类东西（协作规则/思维方式/环境适配/编码规范/术语表/流程），而协议按手册的速度增长 → 宪法被稀释 | 生效中（ADR-0006） |
| 2026-09-16 | 第一批降级：`A2`→并入 `W2`、`A8`→并入 `patterns/design-decision`、`A9`→并入 `patterns/cross-domain-borrowing`、`A5`→**删除** | 三条与已有 pattern/workflow **逐字重复**（有素材证明）；A5 可表演、零约束力 | 已完成（约定 19→15，validate 103/0） |
| 2026-09-16 | 新增约定的**门槛**：必须能回答"不看它，一个称职的通用模型会做错吗" | 答不上来 = 把模型本来就会的东西冻结成维护债 | 生效中 |
| 2026-09-16 | **建立集成层 `integrations/`**：协议层留跨环境不变的规则，环境适配单独一层（**分层，不是分版**） | 分两个版本会把协议层和适配层一起复制 → 又分叉 | 生效中（ADR-0007） |
| 2026-09-16 | 用户确认：**对话式 AI（无文件权限）是真实场景，不是历史包袱** | 决定 `integrations/` 要同时容纳 `chatgpt-like` 与 `cli-agent` 两类环境 | 已确认 |
| 2026-09-16 | `A7` 拆分：AI 写权限边界 → `integrations/cli-agent-boundaries`（改写为**可执行版**）；社区治理 → 归档 | 原文"AI 永远不持有写权限"**与事实相反**；不可执行的规则会稀释全部规则的权威 | 已完成 |
| 2026-09-16 | `A16` 前提修正：FIFO/机械截断 → **不可控压缩函数**；删"每 10 轮复述核心约束" | 基于错前提的纪律会让 AI 做无效动作；新规则"重要到不能被压缩的东西必须写进文件"**可自查** | 已完成 |
| 2026-09-16 | 工具侧新增 `EntryKind.Integration`（`integrations/` 成为可校验、可索引的一等目录） | 否则集成层不受 validate 约束 —— 又会变成"没人检查的规则" | 已完成 |
| 2026-09-16 | **约定层收敛完成：19 → 8**（`A3 A4 A6 A10 A12 A13 A14 A16`） | 每条都过四问：是协作规则吗 / 能自查吗 / 前提准确吗 / 重复吗 | 已完成（ADR-0008） |
| 2026-09-16 | `A11` 降级为 `patterns/value-semantics`（不是删） | 它不绑语言/栈，是通用判据（"跨域共享同一个零会摧毁语义系统"）—— 模型未必知道；删了就丢了 | 已完成 |
| 2026-09-16 | `A19` 的**配额棘轮**删除（"至少 20 轮/2 轮/3 个/1 个"） | 配额不可自证 → 只会被表演。判据改为**质量**（有没有更深一层），不是**数量**（跑了几轮） | 已完成 |
| 2026-09-16 | `A1` 并入 `S1`、`A18` → `meta/terminology`、`A19` → `workflows/W11` | 逐字重复 / 术语表 / 执行流程 | 已完成 |
| 2026-09-16 | **下一次动基座必须先写迁移脚本** | 这两轮改了链接规则、目录拓扑、id 规则，全靠 validate 兜底 —— 那篇文章说的"冻结基座"我们还没做到 | 待执行 |
| 2026-09-16 | **id 是不可变快照，永不重编号**（缺口即死亡记录；身份=编号，描述=语义后缀） | 用户提出：修改原 id 就修改了真相。重编号会连带 ADR / 日志 / `git log` / 外部引用一起失效 | 生效中（ADR-0009） |
| 2026-09-16 | `catalog.json` **用生成，不手写**（`collab catalog`） | 用户原话："永远不要相信不是自动化和规范的行为" —— 手写索引就是第二份真相源 | 已完成 |
| 2026-09-16 | **链接锚点选定 id**（不可变快照）；id 与文件名都可解析，推荐 id | 实测澄清：id 断链的成因不是"id 不能用"，而是"**id 没登记进 `aliases`**" —— 缺的是验收，不是能力 | 生效中 |
| 2026-09-16 | 新增验收规则 `ID_NOT_IN_ALIASES`：`frontmatter.id` 必须在 `aliases` 里 | 把"渲染层能解析 id"从**假设**变成**不变量**；忘了写就静默断链，而旧校验器查不出来 | 生效中 |
| 2026-09-16 | 不把规则建立在"用户恰好在 Obsidian 里改名"这个前提上 | Obsidian 改名自动改写链接，git / CLI / 静态站点不会 —— 锚点要选在任何工具里都不变的那个 | 生效中 |
| 2026-09-16 | **工具自动写机械字段**（用户拍板）：`aliases` 里的 id 由工具写，其余别名归人 | 手工录入繁琐且易错；而 id / 别名 / 作者 / 类型是系统最重要的东西，必须保证正确。**尊重手动录入作为辅助** | 生效中 |
| 2026-09-16 | `collab new` 自动写入 `aliases: [id]` | 补一个真实漏洞：**工具生产的文件曾违反工具自己的新规则**（`ID_NOT_IN_ALIASES`） | 已完成 |
| 2026-09-16 | 新增 `patterns/reproducible-verification`（可复现的证据优于手工验证） | 用户问"工程化那条要不要进库" —— 它是**工程判据**，按定义归 patterns 不归 agreements | 已完成 |
| 2026-09-16 | **`collab fix` 只补不删 + 报错**（用户拍板） | 补是机械事实（id 必须登记为 alias），删是判断 —— **判断归人**。补不了就报错，不猜、不覆盖 | 生效中 |
| 2026-09-16 | `collab fix` 的判据**复用领域规则 `idIsAlias`** | 否则会出现"校验说缺、修复说不缺"的两份真相 | 已完成 |
| 2026-09-16 | **`CATALOG_STALE` 进 validate**（用户拍板："validate 本来就是为了正确的构建"） | catalog 是生成物，过期即"与工作区不一致"，与死链/缺索引同类。对比时**忽略 `generatedAt`** | 已完成 |
| 2026-09-16 | catalog 检查放在 **application 层**（不是 domain/rules） | 它组合了 `buildCatalog`（应用层）—— domain 不得 import application（ESLint 分层规则当场抓出我放错层） | 已完成 |
| 2026-09-16 | `catalog.json` **存在才检查**（`null` 不报） | 不是每个工作区都需要 catalog；删掉它是**可见行为**（git 里看得见） | 生效中 |
| 2026-09-16 | **谁让派生物过期，谁负责刷新它**：`apply` 落盘后**自动**刷新 `catalog.json` | 否则 `apply --commit` 必然卡在 `CATALOG_STALE`。catalog 是 100% 生成物（无人工内容），无条件刷新是安全的 | 已完成 |
| 2026-09-16 | `extractLinks` **跳过围栏代码块与行内代码** | 旧行为把"讨论链接"的文档判成死链（写 ADR-0009 时踩了两次）；旧的"已知限制"测试已翻转为"不再检测" | 已完成 |
| 2026-09-16 | **装上代谢机制**：`collab new agreement` 在 active ≥ 10 时**拒绝**（无 `--force`） | 全库唯一的"环境型"机制：让"加"包含"减"的代价。**绕过它的成本必须高于遵守它的成本** —— 便宜的逃生口会让它退化成仪式 | 生效中 |
| 2026-09-16 | **`dormant`/`deprecated` 退出路由索引**（`buildCatalog` 过滤） | 五篇文章一致主张：dormant 的正确语义不是"灰掉仍占位"，而是**从路由索引除名**。死亡成本归零，抵抗删除才没理由 | 已完成 |
| 2026-09-16 | **冻结基座**：新增 `meta/base-contract.md`，规定"动基座 = 破坏性变更，必须 ADR + 迁移脚本 + validate 归零" | 一整个会话改了 4 次基座，4 次都没迁移脚本，全靠 validate 事后兜底 —— 那是运气不是设计 | 生效中 |
