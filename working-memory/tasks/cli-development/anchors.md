# Anchors: CLI Development

> 当前任务的关键约束。**每次新对话必读**。

## 不可遗忘的约束

1. **流程**：遵守 A10（Spike → 规格 → 测试 → 实现 → Review）。
2. **边界**：`src/domain/` 不 import 第三方（S12）。
3. **反馈**：每次复杂交互附知识笔记（A12）。
4. **上下文**：按需读取，不全量（A16）。
5. **校验**：产物必须通过 `collab validate`（无 index 相关规则）。

## 当前阶段

`collab new` 已完成 → 准备进入 `collab index`。

## 下一个动作

`collab index` 的 Spike（2 小时 Timebox）。

# Decisions

> 决策日志——记录"何时做了什么决定"的历史。

| 日期 | 决策 | 理由 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-09-14 | 采用 `ExitCode` 常量替代魔法数字 `0` / `1` | 值同不代表语义同（A11） | 待实现 |
| 2026-09-14 | 用 `useTestWorkspace` 封装通用 Arrange | What/How 分离原则 | 待实现 |
| 2026-09-14 | 命令 fixture 用具名工厂（`writeSkill` 等） | 让 What 显式化 | 待实现 |
| 2026-09-14 | Act / Assert 永不封装 | 它们是"测试的身份" | 生效中 |
| 2026-09-14 | CLI 采用渐进式 DDD，不做一次性重构 | 目标结构未验证 | 生效中 |
| 2026-09-14 | 分两批回归：先测试（批次 1），后代码（批次 2） | 测试是代码的安全网 | 生效中 |