# CLI 长运行循环

**更新**：2026-09-17 09:40 ｜ **tick**：2 完成

## tick 2 摘要

| 交付 | 结果 |
| :--- | :--- |
| `.github/workflows/ci.yml` | PR + push(main/collab-new) + workflow_dispatch |
| `npm run check` | 唯一门禁（去掉重复 build） |

## tick 3 焦点

- `patterns/_index.md` 32 行空白列补名称，或整理 extra 分区重复
- collaboration 侧 validate CI（可选）

## 停止

用户说「停止长运行」→ 不再 arm wake。
