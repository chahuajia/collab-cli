# Anchors: D 实验

**更新**：2026-09-16 22:35

> 当前任务的关键约束。**每次新对话必读**。

## 不可遗忘的约束

1. **判据表冻结**：`_experiment\round-3-experiment.md` 第一节那张表，**跑完不许改**。
2. **单变量**：两臂 brief **逐字相同**（只差路径）；差异**只能**是"`AGENTS.md` 里有没有知识库那一段"。
3. **串行 + 同一路径**：一次只跑一臂；运行目录恒为 `D:\swap-station`。
4. **不在被试窗口跑命令**：投递 = **粘贴 brief 正文**。
5. **先跑装置自检**：`node run-round3.mjs check` —— 不干净就别开跑。
6. **判据不许只活在对话里**：判据 / 基线 / 驱动器都必须落文件。
7. **AI 不 commit、不 push 被试仓库。**

## 可自查的判据

> 我用的这条规则，来自**文件**还是来自**对话**？来自对话的约束 = 未来会消失的约束。