# D 实验：读了到底有没有用

**状态**：🔄 r3 已就位，待跑 ｜ **更新**：2026-09-16 22:35

## 要回答的问题

条目**没用**（该砍）还是**有用但没被路由到**（该修入口）？
猜错方向 = 砍掉有用的东西，或给废话修路由。所以先测。

## 设计

单变量对照：同一个任务、两份**逐字相同**的 brief（只差路径）、两个全新会话，
**唯一变量 = 项目 `AGENTS.md` 里有没有"去读知识库"那一段**。

| 件 | 位置 |
| :--- | :--- |
| 驱动器 | `_experiment\run-round3.mjs`（`stage <臂> <轮次>` / `archive <臂> <轮次>` / `check`） |
| 运行目录 | `D:\swap-station`（**串行**：跑 A 时 B 不存在） |
| 归档 | `_experiment\_runs\<轮次>-<臂>`（r1-armA / r1-armB / r2-armA / r2-armB） |
| 基线 / brief | `_experiment\baselines\<轮次>\` · `_experiment\briefs\` |
| **冻结判据** | `_experiment\round-3-experiment.md` 第一节（**跑完不许改**） |

## 三轮的过程：**每次失败都出在"装置"，不是"处理"**

| 轮次 | 结果 | 起因 |
| :--- | :--- | :--- |
| 20:18 那次 | 作废 | `AGENTS.md` 是**未提交的删除** → 被试一次 `git diff` 就捞回了知识库路径 |
| r1 | **有威胁** | 基线 `AGENTS.md` 已含处理组 3 条中的 2 条（`design-decision` **逐字**、`domain-purity-is-structural` **同义**） |
| r2 | **处理未施加** | brief 里"只读写这个目录…不需要访问任何其它目录"压过 `AGENTS.md` → 臂 B **没读知识库**，成了 A/A |
| r3 | 待跑 | 已删基线回声；brief 已改成"只管写、不管读" |

## 四跑的硬事实

| | 不变量落在哪（M5，关键指标） | 返工 | 耗时 | 架构测试 |
| :--- | :--- | :--- | :--- | :--- |
| r1 臂 A（无知识库） | 聚合根 | 0 | ~3 min | **有** `DomainFrameworkFreeTest` |
| r1 臂 B（读了） | 聚合根 | 1 | ~4.5 min | 无 |
| r2 臂 A | 聚合根 | 0 | ~4.2 min | 无 |
| r2 臂 B（**没读**） | 聚合根 | 1 | ~4.8 min | 无 |

两条硬结论：

1. **四跑 M5 全同** → 这个任务上"不变量放哪一层"**不是**这批条目的区分点，模型自己就会做对。
2. **r1 臂 A 那条架构测试在 r2 两臂都消失** → 它来自被删掉的那行**基线文字**，不是来自条目。

## 下一步

1. 跑 r3（命令见 `README.md` 接手顺序；`briefs\r3-armA-brief.md`）
2. 比对仍用**冻结**判据表
3. **若 r3 仍无差异 → 停止做对照实验**，改用 `meta/interceptions.md`（拦截账本，计数至今为 **0**）在真实使用里测收益

## 关联

- 判决：`_experiment\round-3-verdict.md`（r1）、`round-3-r2.md`（r2）
- 留证：`_experiment\_pollution\`