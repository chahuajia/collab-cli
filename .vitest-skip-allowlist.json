{
  "$comment": "被允许跳过的测试。跳过 = 没验，不是验过了 —— 每一条都必须写明为什么可以跳过，以及什么条件下应该删掉这一条。机制见 scripts/assert-no-skips.mjs。",
  "allowed": [
    {
      "file": "src/mcp/__tests__/tools.test.ts",
      "name": "真库",
      "reason": "依赖真库路径（COLLAB_REAL_KB 或本机默认位置）。本机没有该仓时只能跳过。2026-09-26 起 CI 已 checkout 真库并设 COLLAB_REAL_KB（不再是静默 skip）；这一条只剩本机兜底。"
    },
    {
      "file": "scripts/__tests__/kb-templates.test.ts",
      "name": "真库",
      "reason": "同上：要读 KB 的 templates/ 才能验“抄出来能过 validate”。CI 已接线（COLLAB_REAL_KB），本机无 KB 时兜底。"
    },
    {
      "file": "scripts/__tests__/kb-command-docs.test.ts",
      "name": "真库",
      "reason": "同上：要读 KB 的文档才能比对「文档 ↔ --help」。CI 已接线（COLLAB_REAL_KB），本机无 KB 时兜底。"
    }
  ]
}
